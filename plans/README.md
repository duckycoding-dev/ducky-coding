# Implementation Plans

Generated by the improve skill on 2026-07-03, against commit `1fce5b5`
(branch `develop`; SHA taken from the session's git snapshot — the advisor
session was barred from running git commands). Execute in the order below
unless dependencies say otherwise. Each executor: read the plan fully before
starting, honor its STOP conditions, and update your row when done.

Advisor constraints honored: no source code was modified, no git/build/test
commands were run; every finding excerpt was verified by direct file reads.

**Status re-verified 2026-08-08 at `f653ed9`** by re-running each plan's
claims against the working tree. No plan has been executed. Two are
partially resolved by unrelated work; see "Changes since the audit" below.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | CI workflow (check/lint/format on push+PR) | P1 | S | — | DONE |
| 002 | Vitest harness + characterization tests | P1 | M | — (001 recommended first) | DONE — 43 tests in 4 files; 3 KNOWN BUG tests for 003/006 to flip |
| 003 | Make build-time DB sync non-destructive and gateable | P1 | M | 002 | DONE — transaction scoped to cleanup only (Turso 5s interactive-tx limit); build-flow.md paragraph left to 007 |
| 004 | Dependency security bumps + declare phantom `yaml` dep | P1 | S | — (001 recommended first) | DONE — 59 → 21 advisories, 0 critical; rest blocked on the Astro 7 major (DEP-003) |
| 005 | Fix corrupted .gitignore, untrack content.db/-wal | P2 | S | — | DONE |
| 006 | Five small confirmed correctness fixes | P2 | S | 002 recommended | DONE — Fix C had a second occurrence in `topics/index.astro`; all 3 memes already had `createdAt` |
| 007 | Docs truth sweep (SSR search, dead API docs, CLAUDE.md) | P2 | S | — (coordinate w/ 003, 006) | TODO — plus new `src/data/` to document |
| 008 | /search CDN cache: investigate durable semantics, right-size | P2 | S | — | TODO |
| 009 | DX hygiene (.env.example, README, engines, packageManager) | P2 | S | — | DONE — `DB_SYNC` line added to `.env.example` by 003 |
| 010 | Perf smalls (batch image queries, glob hoist, dead public images) | P3 | S | — | DONE |
| 011 | Delete 17 dead markdown Custom* wrappers | P3 | S | — | DONE |
| 012 | Memes repository/service + service naming consistency | P3 | M | 006 recommended first | TODO |
| 013 | Content roadmap (direction — the highest-leverage item) | P2 | S doc / ongoing writing | topic pruning blocked on 003 | TODO — cross-linking evidence partly overtaken by `fb28acb` |

Status values: TODO | IN PROGRESS | PARTIAL | DONE | BLOCKED (with one-line
reason) | REJECTED (with one-line rationale).

## Changes since the audit (`1fce5b5` → `f653ed9`)

Read this before executing any plan; each affected plan's "Current state"
section has been updated in place.

- `c9dc138` — merge of `develop` into `main` that **deleted five components**
  still imported across the site (`card/Card.astro`, `tag/Tag.astro`,
  `navbar/Navbar.astro`, `layouts/header/Header.astro`,
  `memes/MemeExternalLinks.astro`). Cause: case-only directory renames
  (`Card/` → `card/`) resolved by hand on a case-insensitive filesystem.
  `astro check` failed with 55 errors on both branches until `5ac89ce`
  restored them. The same merge repaired the concatenated `.gitignore` line
  (half of plan 005).
- `ef5d565` — README expanded from 5 to 50 lines (part of plan 009).
- `fb28acb` — homepage and `/my-projects` repositioned as Frontend Engineer;
  project data extracted to `src/data/projects.ts` behind a new `@data/*`
  alias; new `components/card/ProjectGrid.astro`; `json-ld/person.ts`
  retitled; `Link.astro`'s `outlined` `::before` anchored with `inset-0`
  (removing two padding-coupled compound variants).
- Still no test runner, so none of the above was regression-tested — plan
  002 remains the gate for 003 and 006.

## Dependency notes

- **003 requires 002**: plan 002 writes characterization tests (including two
  deliberately-named KNOWN BUG tests) that plan 003 flips — the sync rewrite
  is unverifiable without them.
- **006 flips one plan-002 test** (deletedAt); executable without 002 by
  skipping the test edits.
- **007 vs 003/006**: docs sweep should document `DB_SYNC` if 003 landed, and
  skips the `?? 0 > 0` doc example if 006 already fixed it. Either order works.
- **013's topic pruning is blocked on 003**: deleting a topic JSON before the
  sync is fixed exercises the destructive orphan-cleanup path against prod.
- **012 after 006**: plan 006 edits `search.repository.ts` (LIKE escaping);
  012 moves that code. Running 012 first forces 006 to chase moved lines.
- 001 and 004 are independent but make every later plan CI-verified — run
  them early.

## Findings considered and rejected (do not re-audit)

- **Prod sourcemap exposure** (`vite.build.sourcemap: true`): verified NOT
  shipping — zero `.map` files in the public `dist` output; maps exist only
  in `.netlify/build/` server chunks.
- **Font payload**: Inter ships as 2 variable woff2 (~100 KB total, both
  styles used) — fine.
- **`prefetchAll: true`**: hover strategy, tiny script, small site — fine.
- **`clsx`/`cva`/`tailwind-merge` "overlap"**: standard complementary split
  (`src/libs/cn/`) — not duplication.
- **`astro-seo` vs `astro-seo-schema` "overlap"**: meta tags vs JSON-LD —
  complementary, both used.
- **`markdown-it`/`sanitize-html` "unused"**: both used in
  `src/pages/rss.xml.ts` — alive (bumped in plan 004 instead).
- **`.env.development` committed = secret leak**: no — contains only a local
  `file:` URL and an empty token. Keeping it committed is a deliberate
  convenience; plan 009 adds `.env.example` alongside.
- **XSS via reflected search `q`**: astro-seo-schema HTML-escapes JSON-LD
  strings; page output goes through Astro's default escaping — no sink found.
- **Search SQL injection**: Drizzle `like()` parameterizes — only the
  wildcard-semantics bug is real (plan 006 Fix D).
- **9 near-identical social icon wrappers** (`src/components/icons/*-icon/`):
  real boilerplate, but used via component identity at call sites;
  consolidation is low-leverage churn — not worth doing now.
- **`SearchParamsSchema` pageSize>100 fails instead of clamping**: real sharp
  edge, characterized in plan 002; changing it is a product decision the
  owner should make when touching search UX — deliberately not planned.
- **buildSync N+1 batching**: real but premature at 3+3+6 content items;
  deferred to plan 003's maintenance notes.
- **`markdown-elements-example.mdx` orphan at `src/content/` root**: outside
  all collection base dirs, leaks nowhere; delete-or-move is a 30-second
  owner call — folded into no plan deliberately.
- **`astro.config.mjs` TODO cluster** (11 inline TODOs): config-exploration
  notes, harmless; clearing them wholesale adds no value — owner sweep.
- **Video assets (~8.8 MB) + 2 MB post banners**: content-owned; recompress
  when editing those posts (noted in plan 010 maintenance).
