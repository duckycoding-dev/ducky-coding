import { db } from 'astro:db';
import { TopicTagsTable } from './tables/TopicTags';

// https://astro.build/db/seed
export default async function seed() {
  // TODO
  // await db
  //   .insert(TopicTagsTable)
  //   .values([{ name: 'Astro' }, { name: 'React' }]);
}
