---
import { Link } from '@components/Link';
import type { HTMLAttributes } from 'astro/types';
import { cn } from '../../utils/cn';

export interface NavbarProps {}

interface Props extends NavbarProps, HTMLAttributes<'nav'> {}

const { class: className, ...props } = Astro.props;
---

<nav class={cn('md:flex md:flex-row items-center', className)} {...props}>
  <!-- <Link id='abc' href={'/'} variant={'unstyled'}>Unstyled</Link>
  <Link id='abc' href={'/'}>Home</Link>
  <Link href={'/about'} variant={'outlined'}>About me</Link>
  <Link href={'/socials'} variant={'contained'} size={'medium'}>Socials</Link> -->
</nav>
