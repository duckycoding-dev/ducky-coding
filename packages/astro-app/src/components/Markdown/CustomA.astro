---
import { Link } from '../Link';

const { props } = Astro;
---

{
  /* used inline style to override text decoration from Tailwind's typography plugin */
}
<Link variant='default' {...props} style={{ textDecoration: 'none' }}>
  <slot />
</Link>
