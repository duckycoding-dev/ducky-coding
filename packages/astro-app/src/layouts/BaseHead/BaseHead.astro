---
import type { HTMLAttributes } from 'astro/types';

export interface BaseHeadProps {};

interface Props extends BaseHeadProps, HTMLAttributes<"meta"> {}
const {title, class: className, ...props} = Astro.props;

---

<title>{title}</title>
{/* Other meta tags */}