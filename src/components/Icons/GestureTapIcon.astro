---
import GenericIcon from './GenericIcon.astro';
import type { CustomIconProps } from '.';

type Props = CustomIconProps;
const { name, title, ...props }: Props = Astro.props;
---

<GenericIcon
  name={name ?? 'mdi:gesture-tap-button'}
  title={title ?? 'Up arrow caret'}
  {...props}
/>
